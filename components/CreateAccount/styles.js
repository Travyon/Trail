import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        width: '100%',
        height: '100%',
    },

    title:{
        marginTop: 185,
        fontSize: '30px',
        fontWeight: '700',
        textAlign: 'center'
    },

})

export default styles;